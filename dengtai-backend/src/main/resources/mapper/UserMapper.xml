<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.auth.user.UserMapper">

    <resultMap id="UserResultMap" type="org.example.auth.user.User">
        <id column="id" property="id"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="bio" property="bio"/>
        <result column="tags_json" property="tagsJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByPhone" parameterType="string" resultMap="UserResultMap">
        SELECT *
        FROM users
        WHERE phone = #{phone}
        LIMIT 1
    </select>

    <select id="findByEmail" parameterType="string" resultMap="UserResultMap">
        SELECT *
        FROM users
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="existsByPhone" parameterType="string" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM users
            WHERE phone = #{phone}
        )
    </select>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM users
            WHERE email = #{email}
        )
    </select>

    <insert id="insert" parameterType="org.example.auth.user.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            phone,
            email,
            password_hash,
            nickname,
            avatar,
            bio,
            tags_json,
            created_at,
            updated_at
        ) VALUES (
            #{phone},
            #{email},
            #{passwordHash},
            #{nickname},
            #{avatar},
            #{bio},
            #{tagsJson},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <select id="findById" parameterType="long" resultMap="UserResultMap">
        SELECT *
        FROM users
        WHERE id = #{id}
    </select>

    <update id="updatePassword">
        UPDATE users
        SET password_hash = #{passwordHash},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
